<%if @user_id == 1%>
    <form action='/add' method="GET">
        <button type='submit'>Add Game</button>
    </form>
<%end%>

fixa detta!!!
@game = db.execute("SELECT * FROM game WHERE id = ? ORDER BY game_name ASC", @game_genre['game_id'])
<% @game.each do |names|%>
    <h2><a href="/home/<%= @genres %>/<%= names['id']%>"><%= h names['game_name'] %></a></h2>
<%end%>
